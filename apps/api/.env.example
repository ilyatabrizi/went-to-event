# The API uses the in-memory repository when these are absent.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=replace-me

# Optional API server settings
HOST=127.0.0.1
PORT=3000
